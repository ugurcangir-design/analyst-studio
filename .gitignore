# ── Gizli / Kimlik Bilgileri ──────────────────────────────────────
.env
users.json

# ── Python ────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.Python

# ── Çalışma Zamanı Verileri ───────────────────────────────────────
input/
output/
logs/
history/

# ── Atlassian Sync (şirkete özel, büyük boyutlu) ──────────────────
reference/confluence/
reference/jira/
reference/services/
reference/ui-code/
reference/live-app/
reference/current-brd/

# ── macOS ─────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Büyük Dokümantasyon Dosyası (md versiyonu yeterli) ────────────
sistem_promptlari.pdf

# ── IDE / Editör ──────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
_launcher_bin

# ── Claude Code (kullanıcıya özel) ───────────────────────────────
.claude/settings.local.json

# ── API çıktı önbelleği ──
.api_cache/

# ── Makineye özel çalışma-zamanı config (pull çakışmasını önler; .example seed edilir) ──
reference/context_filter.json
reference/prompts.json
reference/sources.json
# Domain kuralları — gizli el kitabı/BRD'den damıtılan domain bilgisi içerir → git'e GİRMEZ
reference/domain-kurallari.md

# ── Canlı uygulama (Chrome MCP): tarayıcı oturumu + üretilen MCP config ──
.live-app-profile/
.mcp.live-app.json

# ── Agent araç artefaktları ──
# @playwright/mcp sayfa snapshot çıktıları (runtime)
.playwright-mcp/
# .claude/skills ile birebir mükerrer (bkz. AGENTS.md)
.agents/

# Backlog Senkron runtime (yüklenen/üretilen Excel'ler)
backlog/

# Makineye özel analist kimliği (telemetri)
analist.json

# App-üretimi RAG cache
reference/_filtered_cache/

# Makineye özel kod kaynağı repo yolu (Faz 3; .example seed edilir)
reference/kod_kaynagi.json

# Analiz veri kaynağı MCP (Faz 3.c; bağlantı dizesi içerir) + üretilen config
reference/analiz_mcp.json
reference/jira_kopru.json
.mcp-analiz.json

# Owner konsol kilidi (Kullanım Raporu + Yetki ekranları) — SADECE owner makinesinde true;
# git'e gitmez, .env'e konmaz (kopyalanan .env sızmasın), .example'dan false seed edilir.
reference/owner_konsol.json

# create_app.sh tarafından üretilen makineye-özel başlatıcı (yol+port içerir)
_start.sh
output/.gozlem-durum.json

# Canlı-gözlem ajanının cwd (repo kökü) network/response dump artıkları — izlenmez
/resp*.json
/fixture_resp*.json

# Canlı-gözlem (playwright) snapshot dump'ları — cwd'ye düşer, ŞİRKET UI içeriği; izlenmez
/nav.yml
/*-modal.yml
/snapshot*.yml
/*.snapshot.yml
