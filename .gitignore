# ── Gizli / Kimlik Bilgileri ──────────────────────────────────────
.env
users.json

# ── Python ────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.Python

# ── Çalışma Zamanı Verileri ───────────────────────────────────────
input/
output/
logs/
history/

# ── Atlassian Sync (şirkete özel, büyük boyutlu) ──────────────────
reference/confluence/
reference/jira/
reference/services/
reference/ui-code/
reference/live-app/
reference/current-brd/

# ── macOS ─────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Büyük Dokümantasyon Dosyası (md versiyonu yeterli) ────────────
sistem_promptlari.pdf

# ── IDE / Editör ──────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
_launcher_bin

# ── Claude Code (kullanıcıya özel) ───────────────────────────────
.claude/settings.local.json

# ── API çıktı önbelleği ──
.api_cache/

# ── Makineye özel çalışma-zamanı config (pull çakışmasını önler; .example seed edilir) ──
reference/context_filter.json
reference/prompts.json
reference/sources.json

# ── Canlı uygulama (Chrome MCP): tarayıcı oturumu + üretilen MCP config ──
.live-app-profile/
.mcp.live-app.json

# ── Agent araç artefaktları ──
# @playwright/mcp sayfa snapshot çıktıları (runtime)
.playwright-mcp/
# .claude/skills ile birebir mükerrer (bkz. AGENTS.md)
.agents/

# Backlog Senkron runtime (yüklenen/üretilen Excel'ler)
backlog/

# Makineye özel analist kimliği (telemetri)
analist.json
